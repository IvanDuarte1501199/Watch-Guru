services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: watchguru
      POSTGRES_PASSWORD: watchguru
      POSTGRES_DB: watchguru
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U watchguru']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
